#include "main.h"

/**
 * swap_int - swaps the values of two integers
 * @a: variable a
 * @b: variable b
 * Return: Always 0
 */

void swap_int(int *a, int *b)
{
	int = a;
	int = b;

	*a = int
	*b = int
}
